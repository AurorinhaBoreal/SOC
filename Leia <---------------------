O que baixar para rodar o SOC:
1-Baixar na pasta frontEnd: nodemodule -> npm i
2-baixar na pasta frontEnd: axios -> npm i axios
3-baixar na pasta frontEnd: sass -> npm i sass
4-baixar na pasta api:      nodemon -> npm i -g nodemon

Como rodar o SOC:

.frontEnd{
  entre na pasta frontend e execute os comandos:
  npm run sass
  npm run watch
  npm start
}

Back-End(
  rodar o banco de dados(soc_db.sql) no MysqlWorkbench

  no arquivo SOC > api > src > repository > conectmysql.js é preciso fazer mudanças de acordo com a sua conexão mysql,
    host:'localhost', <-- mudar
    port: 3306,
    password: 'root1234', <-- mudar
    database:'DB_soc',
    user:'root', <-- mudar

  após os passo anteriores, entre na pasta api pelo terminal e execute o comando:
  npx nodemon src/index.js
);

se der certo não terá erros 😗


